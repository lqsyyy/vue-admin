const config = {
  // baseURL: process.env.NODE_ENV === 'production' ? 'http://www.langlib.com' : '/api'
  baseURL: process.env.NODE_ENV === 'production' ? '' : 'https://easy-mock.com/mock/5950a2419adc231f356a6636/vue-admin'
}

export {
  config
}
